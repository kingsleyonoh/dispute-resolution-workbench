# Clojure / JVM
.cpcache/
.nrepl-port
target/
classes/
*.class
*.jar

# Node / Tailwind tooling
node_modules/
resources/public/assets/*.css.map

# Local config and secrets
.env
.env.*
!.env.example
dev.edn.local

# Datomic / local data
data/
storage/

# Docker / logs
*.log
tmp/

# AI Workflow System (proprietary - do not publish)
# TRACKED DURING DEV - run /prepare-public before making repo public
# .agent/workflows/
# .agent/guides/
# .agent/agents/
.agent/.last-sync

# Internal project planning
# TRACKED DURING DEV - run /prepare-public before making repo public
# docs/progress.md
# docs/yolo-inbox.md
# docs/build-journal.md
# docs/build-journal/
docs/architect_journal.md
# docs/*PRD*.md
# docs/*prd*.md

# YOLO Mode workspace - audit trail
# TRACKED DURING DEV - run /prepare-public before making repo public
# NEVER write production API keys, tokens, passwords, or real credentials here.
# .yolo/

# Multi-agent platform context
# CLAUDE.md / AGENTS.md / .cursorrules are thin pointer files regenerated by /sync-context - leave ignored
# Rules and knowledge are NOT mirrored into .claude/ - pointer files reference .agent/rules/_index.md directly
CLAUDE.md
AGENTS.md
.cursorrules
# TRACKED DURING DEV - run /prepare-public before making repo public
# .claude/
.claude/settings.local.json
.claude/projects/
.claude/ide/
.claude/shell-snapshots/
.claude/todos/
.claude/statsig/
